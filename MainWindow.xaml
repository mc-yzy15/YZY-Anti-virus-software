<Window x:Class="YZYAntiVirus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YZYAntiVirus"
        mc:Ignorable="d"
        Title="YZY杀毒软件" Height="600" Width="800"
        Background="#f0f0f0"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980b9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1f618d"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Background" Value="#3498db"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#95a5a6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Foreground" Value="#2ecc71"/>
            <Setter Property="Background" Value="#e0e0e0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="0">
                            <Grid>
                                <Rectangle x:Name="PART_Track"/>
                                <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left">
                                    <Rectangle Fill="{TemplateBinding Foreground}"/>
                                </Decorator>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2c3e50" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="YZY杀毒软件" FontSize="24" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Border>
        
        <!-- 主内容区 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧扫描选项 -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="5" Padding="10">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Direction="315" Color="#cccccc" Opacity="0.5" BlurRadius="4"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="扫描选项" Style="{DynamicResource HeaderText}" Margin="0,0,0,10"/>
                    <Button Content="快速扫描" Click="QuickScan_Click"/>
                    <Button Content="全盘扫描" Click="FullScan_Click"/>
                    <Button Content="自定义扫描" Click="CustomScan_Click"/>
                    <Button Content="引导扇区扫描" Click="BootSectorScan_Click"/>
                    <Separator Margin="0,10"/>
                    <TextBlock Text="病毒库更新" Style="{DynamicResource HeaderText}" Margin="0,0,0,10"/>
                    <Button Content="检查更新" Click="CheckUpdate_Click"/>
                    <Separator Margin="0,10"/>
                    <TextBlock Text="设置" Style="{DynamicResource HeaderText}" Margin="0,0,0,10"/>
                    <Button Content="扫描设置" Click="Settings_Click"/>
                </StackPanel>
            </Border>
            
            <!-- 右侧扫描结果 -->
            <Border Grid.Column="1" Background="White" CornerRadius="10" Margin="5" Padding="10">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Direction="315" Color="#cccccc" Opacity="0.5" BlurRadius="4"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="扫描结果" Style="{StaticResource HeaderText}"/>
                    <ProgressBar x:Name="ScanProgressBar" Value="0"/>
                    <TextBlock x:Name="ScanStatusText" Text="准备就绪" Margin="5"/>
                    <ListBox x:Name="ScanResultsListBox" Height="*" Margin="5" Background="#f9f9f9" BorderThickness="1" BorderBrush="#e0e0e0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="清除所选" Click="CleanSelected_Click"/>
                        <Button Content="全部清除" Click="CleanAll_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#ecf0f1" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <TextBlock x:Name="StatusBarText" Text="保护状态: 已启用" Margin="5"/>
                <TextBlock Text="上次扫描: 从未扫描" Margin="5" HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
